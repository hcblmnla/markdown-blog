package org.example.mdblog.controller

import org.springframework.web.bind.annotation.RestController

// localhost:8080/{login}/{title}/{postTitle}

// localhost:8080/{login}/topics
// localhost:8080/{login}/{title}/posts

// localhost:8080/{login}/{title}?tag=tag

@RestController
class AuthorController

@RestController
class TopicController

@RestController
class PostController
